# 电机档案：吸尘器电机（一对极，带风机叶片）

## 1. 基本信息

- **用途**：吸尘器驱动
- **负载特征**：风扇叶片，负载转矩 T∝ω^2
- **额定参数**：36V 母线，最高约 72000 RPM，一对极
- **调试时间**：2026 年 8 月

## 2. 电机参数（实测）

| 参数 | 值 | 单位 | 备注 |
|------|------|------|------|
| 极对数 POLES_PAIR | 1 | | 已实测确认 |
| 相电阻 RS | 433 | mΩ | |
| 相电感 L | 231 | uH | |
| d 轴电感 LD | 209 | uH | |
| q 轴电感 LQ | 252 | uH | |

## 3. 踩过的坑及解决方案

### 坑1：启动随机失败——预定位时间不足

- **现象**：按 KEY1 启动后，有时成功有时失败。失败时切入闭环立刻失速停机。
  iq/id 在强拖段稳定正常，一切入就崩。
- **根因**：一对极电机惯量小，预定位阶段转子围绕目标角度做衰减振荡（hunting），
  原预定位时间 1000（≈80ms）不够让振荡衰减完，转子停在振荡中的某个角度就进强拖，
  初角错误导致后续 start_theta_delta 难收敛。
- **解决方案**：预定位时间 1000 → 2000（≈160ms）。改后启动失败问题消失。

### 坑2：强拖失步——目标转速超过同步极限

- **现象**：强拖目标 6000 RPM 时，`start_theta_delta` 满幅抖动/绕卷（不是恒定负载角），
  `foc.speed` 平台值只有 ~3000 RPM。3000 RPM 处有一个"不算收敛的收敛"点。
- **根因**：带风机叶片时负载转矩 T∝ω^2。强拖电流 START_IQ=2000 能提供的电磁转矩恒定，
  转子能同步跟随的稳态速度上限（同步极限）约 3000 RPM。超过就滑极失步。
- **解决方案**：START_IQ 提到 3000 增加转矩裕度，强拖目标设 3000 RPM 让切入发生在
  同步极限附近。

### 坑3：速度环 PI 公式实现错误

- **现象**：速度环调参语义混乱，Kp 像积分、Ki 像比例。
- **根因**：`PIControl_inc_float` 名义是增量式 PI，实际写成 `U = Kp*error + Ki*Δe`
  （比例+微分结构），而非标准 `U = Kp*Δe + Ki*error`。
- **解决方案**：改为标准增量式公式，SPEED_KP/SPEED_KI 数值对调保持等效。

### 坑4：转速上不去——MAX_IQ 限制

- **现象**：目标 12000 RPM 时转速卡在 ~10000 RPM 上不去。`svp.percent` 在 12200~13000
  区间波动（远未饱和到 PERCENT_MAX=60000），`iq_ref` 稳定在 1500。
- **根因**：速度环输出 `iq_ref` 被 MAX_IQ=1500 钳位，电流上不去→转矩上不去→转速上不去。
  不是电压天花板（svp.percent 远未饱和）。
- **解决方案**：调大 MAX_IQ（1500 → 5000）。调大后转速能继续上升。

### 坑5：速度环欠阻尼振荡——增量式 PI 中 KI 太大

- **现象**：稳态运行时给定阶跃（3000→6000 RPM 或 6000→6500 RPM），转速先冲到目标以上
  （如 6954 RPM，超调约 16%）再回落欠调（如 5613 RPM，约 -6%），来回振荡 1~2 个周期后
  才稳定在目标附近（仍有 ±1% 上下波动）。`cuv.iq` 跟随 `cuv.iq_ref` 良好，
  **`iq_ref` 峰值仅约 1284（远低于 MAX_IQ=6554）**，可排除 MAX_IQ 类根因。
- **根因**：原速度环参数 `SPEED_KP=2000 / SPEED_KI=300`。增量式 PI 中：
  - `KP` 作用在 Δe 上 → 等价于微分增益，**提供阻尼**；
  - `KI` 作用在 e 上 → 真正的积分项，**带 90° 相位滞后**。
  KI 太大叠加速度反馈的低通滞后（`speed_lpf(..., 6554)` 时间常数约 4ms）+ SMO/PLL
  估计延迟，闭环相位裕度被吃光，形成低频欠阻尼振荡。详见
  `diagnostic-cards/speed-loop.md` 坑4。
- **解决方案**：`SPEED_KP=4000 / SPEED_KI=50`（"PI 的 PD 化"——加大 KP 增加阻尼、
  减小 KI 释放相位裕度）。改后振荡消失，稳态转速波动收敛到 ±1% 以内。
- **关键反直觉点**：本次调参方向是**加大 KP 同时减小 KI**——很多人直觉认为"KP 大
  振荡更大"，实际增量式 PI 中 KP 项天然带微分性质，加大反而增加阻尼、抑制振荡；
  KI 才是真正的"急脾气纠偏者"，过大会逼疯系统。

## 4. 最终参数（调试完成后的关键参数值）

| 参数 | 值 | 备注 |
|------|------|------|
| 预定位时间 ORIENT_TIME | 2000 | ≈160ms，原值 1000 不够 |
| 预定位电流 ORIENT_IQ | 2000 | |
| 强拖电流 START_IQ | 2000 | 曾考虑提到 3000 以抬高同步极限（待验证） |
| 强拖目标转速 | 3000 RPM | 匹配当前强拖电流下的同步极限 |
| MAX_IQ | 5000 | 原值 1500 限制最高转速 |
| 速度环 SPEED_KP | 4000 | 加大提供阻尼（原 2000 振荡；3000 是 PI 公式修正后的过渡值） |
| 速度环 SPEED_KI | 50 | 减小释放相位裕度（原 300 振荡） |

## 5. 实测波形特征

- **强拖失步时**：`start_theta_delta` 满幅抖动/绕卷（±30000 来回跳），`foc.speed`
  平台值显著低于强拖目标。
- **3000 RPM 准收敛点**：`start_theta_delta` 短暂稳定在切入窗附近，是当前强拖电流下
  的同步极限临界点。
- **预定位时间不足时**：多次启动的 `start_theta_delta` 曲线行为不同——一条稳定收尾
  （成功），一条密集抖动（失败），取决于进强拖那一刻转子停在振荡波的哪个相位。
- **速度环欠阻尼振荡时**（坑5）：3000→6000 RPM 阶跃后 `foc.speed` 先冲到 ~6954
  RPM（超调约 16%）→ 掉到 ~5613 RPM（欠调约 -6%）→ 来回摆 1~2 个周期后稳定在
  6040~5953 RPM（±1%）。`cuv.iq` 始终紧跟 `cuv.iq_ref`，但 `iq_ref` 峰值仅约 1284
  （远低于 MAX_IQ=6554）——这是和"MAX_IQ 限制"的关键区分点。

## 6. 对照结论

- 与航模电机（`hobby-motor.md`）对照：两者电感差异大（231uH vs 十几uH），但都遇到
  "强拖失步"和"观测器信噪比"问题，根因相同（物理根源一致），只是临界点不同。
  验证了"按物理根源聚合、电机特性作标签"的组织方式的有效性。
