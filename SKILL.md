---
name: foc-debug
description: |
  用于永磁同步电机（PMSM）无感 FOC（矢量控制、无位置传感器）驱动的
  调试方法论。涉及电机转不起来、切入闭环失败、过流停机、观测器（滑膜观测器 SMO /
  锁相环 PLL）失锁或冻结、反电动势估计异常、强拖失步、转速虚高、转速上不去、
  速度环震荡、预定位失败等问题。
  当用户提到 FOC 调试、无感驱动、电机转不起来、过流、失速保护、观测器异常、
  启动切闭环失败、强拖失步、适配不同参数电机等场景时使用本技能。
  本技能只收录已通过实测或严格推导验证的调试思路与方法，采用"现象索引 + 物理根源
  排查卡 + 电机实例档案"的三层结构，便于跨电机类型复用经验。
---

# 永磁同步电机（PMSM）无感 FOC 调试技能

## 适用范围与使用原则

本技能提供**芯片无关、代码无关**的无感 FOC 调试方法论与排查思路。用于 PMSM 无感
矢量控制驱动（观测器通常为滑膜观测器 SMO + 锁相环 PLL + 启动状态机）的调试。
它不绑定任何具体芯片、寄存器、文件路径或固件数值——即使更换控制芯片/驱动板，
本技能的方法与判断思路依然适用；执行时需把"思路"落地为当前项目的具体代码与参数。

## 调用方式（写死不可遗漏）

本技能由 AI 助手在**每一次接到电机 FOC 相关问题（包括调试、参数整定、波形分析、
故障定位、电机适配等任何场景）时自动调用**，按以下流程执行：

1. **先在技能内检索**：根据用户描述的现象/问题，查阅本文件「故障现象索引」与
   `references/diagnostic-cards/` 各排查卡、`references/motor-profiles/` 各电机档案，
   判断该问题是否已被记录，或是否存在相似的已验证案例。
2. **若命中**：直接读取对应排查卡/电机档案，把已验证的判断方法与处理思路告诉用户，
   复用经验、不重新推导。
3. **若未命中**：明确告知用户"本技能中暂未收录该问题/现象"，然后基于当前实际代码
   与实时调试数据，按下方「通用调试总流程」逐步排查。**绝不强行套用未经验证的条目**。
4. **新调试案例完成后**，把已验证的发现（实测过的现象、根因、解决方案、最终参数）
   补充回技能——按"现象 → 排查卡坑条目 → 电机实例档案"的对应位置归档，避免下次
   重复踩坑。

**使用原则（写死不可违反）：**

1. **只采用已验证的结论与方法。** 本技能正式内容只收录已通过实测验证或严格物理推导
   确认的调试思路。任何仅"推导合理、尚未上机"的想法，不得写入正式条目，也不得作为
   结论直接采用。
2. **找不到对应情况时，不强行套用本技能。** 回到实际调试数据和代码本身，按下方
   「通用调试总流程」逐步排查。本技能提供的是已验证的思路和排查框架，不是穷尽所有
   故障的清单。
3. **实测数据永远优先于推测。** 所有理论推导最终都要与实测波形逐位对账，否则不得
   定性为"已解决"。若实测与推导矛盾，以实测为准。
4. **结论与记录/代码矛盾时，以当前实际代码为准重新推导。** 调试记录可能来自旧固件，
   使用前先确认"这份数据测的是哪个固件/哪次修改"。
5. **不写补丁绕过症状，要探究根因。** 如果某个判据频繁误触发，根因往往不在判据本身，
   而在判据之前的前置环节（如预定位、强拖同步、观测器锁定）。先修前置环节，不要靠
   放宽/收紧判据来打补丁。

## 通用调试总流程（换芯片同样适用）

无感 FOC 出问题（转不起来 / 切闭环失败 / 过流 / 转速异常）时，按以下层次逐层排查，
不要跳层、不要打补丁绕过症状：

1. **先量化，再怀疑。** 每改一个参数前先按物理公式手算一遍（离散化系数、死区/管压降
   损失、观测器极限环跳变、反电动势幅值、电流环带宽、采样与串口带宽预算等），算完再
   上机。盲试效率低且不可复现，也拿不到可靠经验。
2. **用"已正常工作的电机"做对照。** 同一套代码、同一块板子上，把问题电机与已知正常
   的电机对比，差异点就是问题所在层次。典型排查顺序：
   采样与调制 → 电流环 → 电压模型（死区/管压降）→ 观测器离散化 → 观测器锁定 →
   启动流程（预定位 / 强拖 / 切入判据）。
3. **区分"观测值"与"被观测对象"。** 观测器输出的转速、角度、反电动势都是"观测结果"
   而不是直接测量。先确认被观测对象（转子实际位置、相电流真实波形）的物理状态，再怀疑
   观测器本身。观测值异常（如转速虚高）不等于物理上真到了那个速度。
4. **区分"穿越检测"与"收敛检测"。** 判据里"连续命中 N 次"只对匀速穿越有效，拦不住
   "振荡逗留"（速度振荡到顶点时角度变化率趋零、会在判据窗口内短暂逗留而误判收敛）。
   要真正区分"已收敛"与"振荡逗留"，必须叠加波动幅度 / 变化率类判据。
5. **先抬信号信噪比，再谈判据。** 观测器"锁到真实转子"还是"锁到强制激励的电流特征上"，
   本质取决于输入信噪比。若反电动势与滑膜切换噪声、电流纹波、死区残余同量级，任何
   判据都会被骗。应先抬高信号（提高强拖速度/加大强拖电流、压低噪声、改善采样），再
   收窄判据。
6. **改动启动流程时，同时观测"电流、速度、角度差"三个变量。** 预定位 / 强拖 / 切入
   闭环这段最容易出问题，三个变量互相印证，比单变量盲猜快得多。
7. **先确认测的是哪个固件。** 使用历史调试数据前，确认它对应哪次代码修改；若数据与
   当前代码逻辑矛盾，多半是旧固件的数据，按当前代码重新核对。

## 故障现象索引（决策树）

调试时从"现象"入口，不要从"电机类型"入口。下表把常见现象映射到对应的排查卡，
命中后加载对应 `references/diagnostic-cards/` 文件按坑逐条排查。

| 现象 | 先查（物理根源） | 排查卡 |
|------|------|------|
| 启动有时成功有时失败 | 预定位时间不足→转子振荡未衰减完→初角错误 | `startup.md` 坑1 |
| 切入闭环立刻失速/过流停机 | 预定位时间不足→初角错误（最常见）→强拖失步→坐标系错位 | `startup.md` 坑1/坑2/坑4 |
| 强拖段角度差满幅抖动 | 转子跟不上旋转磁场（滑极） | `startup.md` 坑2/坑3 |
| 转速卡在某个值上不去 | 速度环输出电流限幅（MAX_IQ） | `speed-loop.md` 坑2 |
| 切入后一快一慢/转速波动 | 切入后弱驱动空窗 + 速度环增益 | `speed-loop.md` 坑3 |
| 稳态阶跃后转速超调 + 衰减振荡（iq_ref 未顶到 MAX_IQ） | 增量式 PI 中 KI 太大 + 反馈低通滞后→闭环相位裕度不足 | `speed-loop.md` 坑4 |
| 观测角度冻结/不旋转 | 电流估计器离散化病态 | `observer-lock.md` 坑1 |
| 观测角度偏离真实转子 | 观测器锁到强制激励而非转子 | `observer-lock.md` 坑2 |
| 转速虚高到物理不可能值 | PLL 积分器无限幅溢出 | `observer-lock.md` 坑3 |
| 电流闭环低频振荡 | 死区/管压降误差与信号同量级 | `sampling-modulation.md` 坑1 |
| 速度环怎么调都不对 | 增量式 PI 公式实现错误 | `speed-loop.md` 坑1 |
| 大惯量负载(风扇/泵)盲定位启动随机失败,加时间/两段式只降失败率不归零 | 固定矢量盲定位角度盲区,非时间问题 | `startup.md` 坑7 |

## 排查卡说明

排查卡按**调试阶段/物理根源**聚合，位于 `references/diagnostic-cards/`。
每张卡里的每个坑给出四要素：**现象 → 物理根源 → 判断方法 → 处理思路**。
每个坑带"适用标签"标注电机特性（如 `L<1mH`、`带风机叶片`），标签是元数据不是目录，
同一经验可被多类型电机复用。

## 电机实例档案说明

电机实例档案按**具体电机实例**记录（不是按类型），位于 `references/motor-profiles/`。
每台电机一份，记录：关键参数、负载特征、实测波形特征、踩过的坑和最终参数。
调试新电机时，先翻档案找最接近的实例做对照，差异点就是排查方向。

## 使用方式

1. 用户提出 FOC 调试问题后，先从「故障现象索引」命中现象，加载对应排查卡。
2. 命中卡内"已验证"的坑 → 按四要素排查。
3. 未命中 → 回到本文件的「通用调试总流程」，基于当前实际代码和实时调试数据逐步排查。
   绝不强行套用未经验证的思路。
4. 调试新电机时，先翻 `references/motor-profiles/` 找最接近的实例做对照。
